name: Drime Native Overlay VM

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

concurrency:
  group: drime-live-vm
  cancel-in-progress: false

jobs:
  drime-live-vm:
    runs-on: ubuntu-latest
    timeout-minutes: 0

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Install packages
      run: |
        sudo apt-get update -y
        sudo apt-get install -y python3 curl qemu-utils qemu-system-x86 wget ca-certificates jq xvfb x11vnc net-tools fuse3
        sudo modprobe fuse || true

    - name: Install latest rclone
      run: |
        curl -fsSL https://rclone.org/install.sh | sudo bash
        rclone version

    - name: Install NetBird
      run: |
        curl -fsSL https://pkgs.netbird.io/debian/public.key | sudo gpg --dearmor -o /usr/share/keyrings/netbird-archive-keyring.gpg
        echo "deb [signed-by=/usr/share/keyrings/netbird-archive-keyring.gpg] https://pkgs.netbird.io/debian stable main" | sudo tee /etc/apt/sources.list.d/netbird.list
        sudo apt-get update -y
        sudo apt-get install -y netbird
        sudo netbird up --setup-key CE303734-75C3-44DC-8E35-B533A858A900
        sudo netbird status

    - name: Configure Drime
      env:
        DRIME: ${{ secrets.DRIME }}
        DRIME1: ${{ secrets.DRIME1 }}
        DRIME2: ${{ secrets.DRIME2 }}
        DRIME3: ${{ secrets.DRIME3 }}
        DRIME4: ${{ secrets.DRIME4 }}
        DRIME5: ${{ secrets.DRIME5 }}
      run: |
        sudo mkdir -p /run/drimelive
        python3 - <<'PY'
        import os, re, subprocess
        if not os.path.exists("drime-storage.conf"):
            open("drime-storage.conf", "w").write("DRIME 100G\n")
        for line in open("drime-storage.conf"):
            line = line.split("#",1)[0].strip()
            m = re.match(r'^(DRIME[0-9]*)\s+(\S+)', line)
            if not m: continue
            name = m.group(1)
            key = os.environ.get(name)
            if not key: continue
            subprocess.run(["rclone","config","delete",name], capture_output=True)
            c = subprocess.run(["rclone","config","create",name,"drime","access_token="+key], capture_output=True)
            if c.returncode==0:
                open("/tmp/drime_remote","w").write(name)
                print(f"Configured: {name}")
                break
        PY

    - name: Mount Drime (FUSE with auto write-back)
      run: |
        REMOTE=$(cat /tmp/drime_remote)
        sudo mkdir -p /mnt/drime
        # vfs-write-back 10s ensures changes are pushed to Drime in the background automatically
        sudo rclone mount "${REMOTE}:liveblock" /mnt/drime \
          --vfs-cache-mode full \
          --vfs-cache-max-size 12G \
          --vfs-read-ahead 128M \
          --vfs-write-back 10s \
          --buffer-size 256M \
          --daemon \
          --allow-other
        echo "Waiting for base vmdk..."
        for i in $(seq 1 30); do
          if ls /mnt/drime/*.vmdk 1> /dev/null 2>&1; then
            echo "Found: $(ls /mnt/drime/*.vmdk 2>/dev/null | head -1)"
            exit 0
          fi
          sleep 1
        done
        echo "ERROR: vmdk not found"
        exit 1

    - name: Create QCOW2 Overlay
      run: |
        BASE=$(ls /mnt/drime/*.vmdk | head -1)
        # Create an overlay that points to the cloud VMDK. 
        # All writes go to this overlay, keeping the base VMDK pristine.
        sudo qemu-img create -f qcow2 -F vmdk -b "$BASE" /mnt/drime/overlay.qcow2
        echo "Overlay created. QEMU will boot from this instantly."

    - name: Start VNC
      run: |
        sudo pkill -f Xvfb||true; sudo pkill -f x11vnc||true; sleep 2
        sudo Xvfb :1 -screen 0 1920x1080x24 & sleep 2
        sudo x11vnc -display :1 -forever -nopw -rfbport 5900 -shared -bg; sleep 2

    - name: Boot VM (Native QCOW2 Overlay)
      run: |
        NB_IP=$(sudo netbird status | grep 'NetBird IP:' | awk '{print $3}' | cut -d'/' -f1)
        [ -e /dev/kvm ] && { sudo chmod 666 /dev/kvm; ACCEL="-enable-kvm"; CPU="-cpu host"; } || { ACCEL=""; CPU="-cpu qemu64"; }
        export DISPLAY=:1; : > /tmp/vm-serial.log
        
        echo "=== BOOTING (Native QCOW2 Overlay) ==="
        sudo -E DISPLAY=:1 qemu-system-x86_64 $ACCEL -machine pc $CPU \
          -smp 4,sockets=1,cores=2,threads=2 -m 16384 \
          -drive file=/mnt/drime/overlay.qcow2,format=qcow2,if=none,id=d0,cache=writeback \
          -device ide-hd,drive=d0 -boot order=c -vga std \
          -netdev user,id=n0 -device virtio-net-pci,netdev=n0 \
          -serial file:/tmp/vm-serial.log \
          -display sdl > /tmp/vm.log 2>&1 &
        QPID=$!
        
        sleep 5
        echo "VNC: $NB_IP:5900"
        ( while kill -0 $QPID 2>/dev/null; do
            sleep 30
            echo "--- serial ---"; sudo tail -n 5 /tmp/vm-serial.log 2>/dev/null || true
          done ) &
        tail -f /dev/null
